node_modules/
.next/
.env.local
.env*.local
*.tsbuildinfo
.convex/
dist/
.DS_Store

# AgentOps session artifacts
.agents/
.gstack/

# Convex tooling sometimes creates a recursive self-symlink
convex/convex

# graphify knowledge graph (local artifact)
graphify-out/

# Secrets + local research data
.env
docs/tagsmith/.env
docs/**/serp.db
docs/**/*.db

# Windows alternate-data-stream junk from WSL/Edge downloads
*:Zone.Identifier

# Real customer PDFs (NDA-sensitive — Boticário, SENAC, APEX bids)
docs/pdf/

# Per-user Claude Code config (hooks, settings)
.claude/hooks/
.claude/settings.json

# Python runtime artifacts (estimator)
**/.venv/
**/__pycache__/
**/.pytest_cache/
**/out/
**/out_*/

# Astro / Node frontend (Astro scaffold on Workers Static Assets)
.astro/
.env.production
*-debug.log*
.idea/

# Wrangler local cache (deploy preview state)
.wrangler/
